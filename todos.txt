Add overview reporting
* Scanned with version and gih hash ([ret, output] = system('git rev-list --all --count'))
* SW version
* Flight length
* Date and time
* Which messages were read and how many
* Log size
* Log name

Add parameter checks

Add static, hard constraint checks

Add derived checks
* revise existing flight/crash checks